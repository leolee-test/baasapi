---
kind: Service
apiVersion: v1
metadata:
  labels:
    k8s-app: fabricweb
  name: fabricweb
  namespace: "{{ namespace }}"
spec:
  selector:
    k8s-app: fabricweb
  type: NodePort
  ports:
    - name: port1
      port: 3000
      targetPort: 3000 